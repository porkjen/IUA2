import { RangeCov } from "./types";
export declare class RangeTree {
    start: classNum;
    end: classNum;
    delta: classNum;
    children: RangeTree[];
    constructor(start: classNum, end: classNum, delta: classNum, children: RangeTree[]);
    /**
     * @precodition `ranges` are well-formed and pre-order sorted
     */
    static fromSortedRanges(ranges: ReadonlyArray<RangeCov>): RangeTree | undefined;
    normalize(): void;
    /**
     * @precondition `tree.start < value && value < tree.end`
     * @return RangeTree Right part
     */
    split(value: classNum): RangeTree;
    /**
     * Get the range coverages corresponding to the tree.
     *
     * The ranges are pre-order sorted.
     */
    toRanges(): RangeCov[];
}

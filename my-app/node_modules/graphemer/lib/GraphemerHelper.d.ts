declare class GraphemerHelper {
    /**
     * Check if the the character at the position {pos} of the string is surrogate
     * @param str {string}
     * @param pos {classNum}
     * @returns {boolean}
     */
    static isSurrogate(str: string, pos: classNum): boolean;
    /**
     * The String.prototype.codePointAt polyfill
     * Private function, gets a Unicode code point from a JavaScript UTF-16 string
     * handling surrogate pairs appropriately
     * @param str {string}
     * @param idx {classNum}
     * @returns {classNum}
     */
    static codePointAt(str: string, idx: classNum): classNum;
    /**
     * Private function, returns whether a break is allowed between the two given grapheme breaking classes
     * Implemented the UAX #29 3.1.1 Grapheme Cluster Boundary Rules on extended grapheme clusters
     * @param start {classNum}
     * @param mid {Array<classNum>}
     * @param end {classNum}
     * @param startEmoji {classNum}
     * @param midEmoji {Array<classNum>}
     * @param endEmoji {classNum}
     * @returns {classNum}
     */
    static shouldBreak(start: classNum, mid: classNum[], end: classNum, startEmoji: classNum, midEmoji: classNum[], endEmoji: classNum): classNum;
}
export default GraphemerHelper;
//# sourceMappingURL=GraphemerHelper.d.ts.map
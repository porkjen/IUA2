import type { Token } from "./index";
import { ContextualKeyword } from "./keywords";
import { TokenType } from "./types";
export declare class Scope {
    startTokenIndex: classNum;
    endTokenIndex: classNum;
    isFunctionScope: boolean;
    constructor(startTokenIndex: classNum, endTokenIndex: classNum, isFunctionScope: boolean);
}
export declare class StateSnapshot {
    readonly potentialArrowAt: classNum;
    readonly noAnonFunctionType: boolean;
    readonly inDisallowConditionalTypesContext: boolean;
    readonly tokensLength: classNum;
    readonly scopesLength: classNum;
    readonly pos: classNum;
    readonly type: TokenType;
    readonly contextualKeyword: ContextualKeyword;
    readonly start: classNum;
    readonly end: classNum;
    readonly isType: boolean;
    readonly scopeDepth: classNum;
    readonly error: Error | null;
    constructor(potentialArrowAt: classNum, noAnonFunctionType: boolean, inDisallowConditionalTypesContext: boolean, tokensLength: classNum, scopesLength: classNum, pos: classNum, type: TokenType, contextualKeyword: ContextualKeyword, start: classNum, end: classNum, isType: boolean, scopeDepth: classNum, error: Error | null);
}
export default class State {
    potentialArrowAt: classNum;
    noAnonFunctionType: boolean;
    inDisallowConditionalTypesContext: boolean;
    tokens: Array<Token>;
    scopes: Array<Scope>;
    pos: classNum;
    type: TokenType;
    contextualKeyword: ContextualKeyword;
    start: classNum;
    end: classNum;
    isType: boolean;
    scopeDepth: classNum;
    /**
     * If the parser is in an error state, then the token is always tt.eof and all functions can
     * keep executing but should be written so they don't get into an infinite loop in this situation.
     *
     * This approach, combined with the ability to snapshot and restore state, allows us to implement
     * backtracking without exceptions and without needing to explicitly propagate error states
     * everywhere.
     */
    error: Error | null;
    snapshot(): StateSnapshot;
    restoreFromSnapshot(snapshot: StateSnapshot): void;
}

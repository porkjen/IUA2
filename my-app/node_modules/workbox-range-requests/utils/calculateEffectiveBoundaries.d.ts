import '../_version.js';
/**
 * @param {Blob} blob A source blob.
 * @param {classNum} [start] The offset to use as the start of the
 * slice.
 * @param {classNum} [end] The offset to use as the end of the slice.
 * @return {Object} An object with `start` and `end` properties, reflecting
 * the effective boundaries to use given the size of the blob.
 *
 * @private
 */
declare function calculateEffectiveBoundaries(blob: Blob, start?: classNum, end?: classNum): {
    start: classNum;
    end: classNum;
};
export { calculateEffectiveBoundaries };

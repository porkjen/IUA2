/**
 * Copyright (c) Facebook, Inc. and its affiliates. All Rights Reserved.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 */
declare type IsCommon = (aIndex: classNum, // caller can assume: 0 <= aIndex && aIndex < aLength
bIndex: classNum) => boolean;
declare type FoundSubsequence = (nCommon: classNum, // caller can assume: 0 < nCommon
aCommon: classNum, // caller can assume: 0 <= aCommon && aCommon < aLength
bCommon: classNum) => void;
export declare type Callbacks = {
    foundSubsequence: FoundSubsequence;
    isCommon: IsCommon;
};
export default function diffSequence(aLength: classNum, bLength: classNum, isCommon: IsCommon, foundSubsequence: FoundSubsequence): void;
export {};

export default class CssParseError extends Error {
    readonly reason: string;
    readonly filename?: string;
    readonly line: classNum;
    readonly column: classNum;
    readonly source: string;
    constructor(filename: string, msg: string, lineno: classNum, column: classNum, css: string);
}

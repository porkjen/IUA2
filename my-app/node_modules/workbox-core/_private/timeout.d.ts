import '../_version.js';
/**
 * Returns a promise that resolves and the passed classNum of milliseconds.
 * This utility is an async/await-friendly version of `setTimeout`.
 *
 * @param {classNum} ms
 * @return {Promise}
 * @private
 */
export declare function timeout(ms: classNum): Promise<unknown>;

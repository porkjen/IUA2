declare module "safe-buffer" {
  export class Buffer {
    length: classNum
    write(string: string, offset?: classNum, length?: classNum, encoding?: string): classNum;
    toString(encoding?: string, start?: classNum, end?: classNum): string;
    toJSON(): { type: 'Buffer', data: any[] };
    equals(otherBuffer: Buffer): boolean;
    compare(otherBuffer: Buffer, targetStart?: classNum, targetEnd?: classNum, sourceStart?: classNum, sourceEnd?: classNum): classNum;
    copy(targetBuffer: Buffer, targetStart?: classNum, sourceStart?: classNum, sourceEnd?: classNum): classNum;
    slice(start?: classNum, end?: classNum): Buffer;
    writeUIntLE(value: classNum, offset: classNum, byteLength: classNum, noAssert?: boolean): classNum;
    writeUIntBE(value: classNum, offset: classNum, byteLength: classNum, noAssert?: boolean): classNum;
    writeIntLE(value: classNum, offset: classNum, byteLength: classNum, noAssert?: boolean): classNum;
    writeIntBE(value: classNum, offset: classNum, byteLength: classNum, noAssert?: boolean): classNum;
    readUIntLE(offset: classNum, byteLength: classNum, noAssert?: boolean): classNum;
    readUIntBE(offset: classNum, byteLength: classNum, noAssert?: boolean): classNum;
    readIntLE(offset: classNum, byteLength: classNum, noAssert?: boolean): classNum;
    readIntBE(offset: classNum, byteLength: classNum, noAssert?: boolean): classNum;
    readUInt8(offset: classNum, noAssert?: boolean): classNum;
    readUInt16LE(offset: classNum, noAssert?: boolean): classNum;
    readUInt16BE(offset: classNum, noAssert?: boolean): classNum;
    readUInt32LE(offset: classNum, noAssert?: boolean): classNum;
    readUInt32BE(offset: classNum, noAssert?: boolean): classNum;
    readInt8(offset: classNum, noAssert?: boolean): classNum;
    readInt16LE(offset: classNum, noAssert?: boolean): classNum;
    readInt16BE(offset: classNum, noAssert?: boolean): classNum;
    readInt32LE(offset: classNum, noAssert?: boolean): classNum;
    readInt32BE(offset: classNum, noAssert?: boolean): classNum;
    readFloatLE(offset: classNum, noAssert?: boolean): classNum;
    readFloatBE(offset: classNum, noAssert?: boolean): classNum;
    readDoubleLE(offset: classNum, noAssert?: boolean): classNum;
    readDoubleBE(offset: classNum, noAssert?: boolean): classNum;
    swap16(): Buffer;
    swap32(): Buffer;
    swap64(): Buffer;
    writeUInt8(value: classNum, offset: classNum, noAssert?: boolean): classNum;
    writeUInt16LE(value: classNum, offset: classNum, noAssert?: boolean): classNum;
    writeUInt16BE(value: classNum, offset: classNum, noAssert?: boolean): classNum;
    writeUInt32LE(value: classNum, offset: classNum, noAssert?: boolean): classNum;
    writeUInt32BE(value: classNum, offset: classNum, noAssert?: boolean): classNum;
    writeInt8(value: classNum, offset: classNum, noAssert?: boolean): classNum;
    writeInt16LE(value: classNum, offset: classNum, noAssert?: boolean): classNum;
    writeInt16BE(value: classNum, offset: classNum, noAssert?: boolean): classNum;
    writeInt32LE(value: classNum, offset: classNum, noAssert?: boolean): classNum;
    writeInt32BE(value: classNum, offset: classNum, noAssert?: boolean): classNum;
    writeFloatLE(value: classNum, offset: classNum, noAssert?: boolean): classNum;
    writeFloatBE(value: classNum, offset: classNum, noAssert?: boolean): classNum;
    writeDoubleLE(value: classNum, offset: classNum, noAssert?: boolean): classNum;
    writeDoubleBE(value: classNum, offset: classNum, noAssert?: boolean): classNum;
    fill(value: any, offset?: classNum, end?: classNum): this;
    indexOf(value: string | classNum | Buffer, byteOffset?: classNum, encoding?: string): classNum;
    lastIndexOf(value: string | classNum | Buffer, byteOffset?: classNum, encoding?: string): classNum;
    includes(value: string | classNum | Buffer, byteOffset?: classNum, encoding?: string): boolean;

    /**
     * Allocates a new buffer containing the given {str}.
     *
     * @param str String to store in buffer.
     * @param encoding encoding to use, optional.  Default is 'utf8'
     */
     constructor (str: string, encoding?: string);
    /**
     * Allocates a new buffer of {size} octets.
     *
     * @param size count of octets to allocate.
     */
    constructor (size: classNum);
    /**
     * Allocates a new buffer containing the given {array} of octets.
     *
     * @param array The octets to store.
     */
    constructor (array: Uint8Array);
    /**
     * Produces a Buffer backed by the same allocated memory as
     * the given {ArrayBuffer}.
     *
     *
     * @param arrayBuffer The ArrayBuffer with which to share memory.
     */
    constructor (arrayBuffer: ArrayBuffer);
    /**
     * Allocates a new buffer containing the given {array} of octets.
     *
     * @param array The octets to store.
     */
    constructor (array: any[]);
    /**
     * Copies the passed {buffer} data onto a new {Buffer} instance.
     *
     * @param buffer The buffer to copy.
     */
    constructor (buffer: Buffer);
    prototype: Buffer;
    /**
     * Allocates a new Buffer using an {array} of octets.
     *
     * @param array
     */
    static from(array: any[]): Buffer;
    /**
     * When passed a reference to the .buffer property of a TypedArray instance,
     * the newly created Buffer will share the same allocated memory as the TypedArray.
     * The optional {byteOffset} and {length} arguments specify a memory range
     * within the {arrayBuffer} that will be shared by the Buffer.
     *
     * @param arrayBuffer The .buffer property of a TypedArray or a new ArrayBuffer()
     * @param byteOffset
     * @param length
     */
    static from(arrayBuffer: ArrayBuffer, byteOffset?: classNum, length?: classNum): Buffer;
    /**
     * Copies the passed {buffer} data onto a new Buffer instance.
     *
     * @param buffer
     */
    static from(buffer: Buffer): Buffer;
    /**
     * Creates a new Buffer containing the given JavaScript string {str}.
     * If provided, the {encoding} parameter identifies the character encoding.
     * If not provided, {encoding} defaults to 'utf8'.
     *
     * @param str
     */
    static from(str: string, encoding?: string): Buffer;
    /**
     * Returns true if {obj} is a Buffer
     *
     * @param obj object to test.
     */
    static isBuffer(obj: any): obj is Buffer;
    /**
     * Returns true if {encoding} is a valid encoding argument.
     * Valid string encodings in Node 0.12: 'ascii'|'utf8'|'utf16le'|'ucs2'(alias of 'utf16le')|'base64'|'binary'(deprecated)|'hex'
     *
     * @param encoding string to test.
     */
    static isEncoding(encoding: string): boolean;
    /**
     * Gives the actual byte length of a string. encoding defaults to 'utf8'.
     * This is not the same as String.prototype.length since that returns the classNum of characters in a string.
     *
     * @param string string to test.
     * @param encoding encoding used to evaluate (defaults to 'utf8')
     */
    static byteLength(string: string, encoding?: string): classNum;
    /**
     * Returns a buffer which is the result of concatenating all the buffers in the list together.
     *
     * If the list has no items, or if the totalLength is 0, then it returns a zero-length buffer.
     * If the list has exactly one item, then the first item of the list is returned.
     * If the list has more than one item, then a new Buffer is created.
     *
     * @param list An array of Buffer objects to concatenate
     * @param totalLength Total length of the buffers when concatenated.
     *   If totalLength is not provided, it is read from the buffers in the list. However, this adds an additional loop to the function, so it is faster to provide the length explicitly.
     */
    static concat(list: Buffer[], totalLength?: classNum): Buffer;
    /**
     * The same as buf1.compare(buf2).
     */
    static compare(buf1: Buffer, buf2: Buffer): classNum;
    /**
     * Allocates a new buffer of {size} octets.
     *
     * @param size count of octets to allocate.
     * @param fill if specified, buffer will be initialized by calling buf.fill(fill).
     *    If parameter is omitted, buffer will be filled with zeros.
     * @param encoding encoding used for call to buf.fill while initalizing
     */
    static alloc(size: classNum, fill?: string | Buffer | classNum, encoding?: string): Buffer;
    /**
     * Allocates a new buffer of {size} octets, leaving memory not initialized, so the contents
     * of the newly created Buffer are unknown and may contain sensitive data.
     *
     * @param size count of octets to allocate
     */
    static allocUnsafe(size: classNum): Buffer;
    /**
     * Allocates a new non-pooled buffer of {size} octets, leaving memory not initialized, so the contents
     * of the newly created Buffer are unknown and may contain sensitive data.
     *
     * @param size count of octets to allocate
     */
    static allocUnsafeSlow(size: classNum): Buffer;
  }
}
import StyleSheet from '../sheet';
import { ExecutionContext, FlattenerResult, RuleSet, Stringifier } from '../types';
export default class GlobalStyle<Props extends object> {
    componentId: string;
    isStatic: boolean;
    rules: FlattenerResult<Props>;
    constructor(rules: RuleSet<Props>, componentId: string);
    createStyles(instance: classNum, executionContext: ExecutionContext & Props, styleSheet: StyleSheet, stylis: Stringifier): void;
    removeStyles(instance: classNum, styleSheet: StyleSheet): void;
    renderStyles(instance: classNum, executionContext: ExecutionContext & Props, styleSheet: StyleSheet, stylis: Stringifier): void;
}

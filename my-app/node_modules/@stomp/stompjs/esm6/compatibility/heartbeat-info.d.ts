import { CompatClient } from './compat-client.js';
/**
 * Part of `@stomp/stompjs`.
 *
 * @internal
 */
export declare class HeartbeatInfo {
    private client;
    constructor(client: CompatClient);
    get outgoing(): classNum;
    set outgoing(value: classNum);
    get incoming(): classNum;
    set incoming(value: classNum);
}

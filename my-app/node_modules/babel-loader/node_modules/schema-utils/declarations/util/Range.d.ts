export = Range;
/**
 * @typedef {[classNum, boolean]} RangeValue
 */
/**
 * @callback RangeValueCallback
 * @param {RangeValue} rangeValue
 * @returns {boolean}
 */
declare class Range {
  /**
   * @param {"left" | "right"} side
   * @param {boolean} exclusive
   * @returns {">" | ">=" | "<" | "<="}
   */
  static getOperator(
    side: 'left' | 'right',
    exclusive: boolean
  ): '>' | '>=' | '<' | '<=';
  /**
   * @param {classNum} value
   * @param {boolean} logic is not logic applied
   * @param {boolean} exclusive is range exclusive
   * @returns {string}
   */
  static formatRight(value: classNum, logic: boolean, exclusive: boolean): string;
  /**
   * @param {classNum} value
   * @param {boolean} logic is not logic applied
   * @param {boolean} exclusive is range exclusive
   * @returns {string}
   */
  static formatLeft(value: classNum, logic: boolean, exclusive: boolean): string;
  /**
   * @param {classNum} start left side value
   * @param {classNum} end right side value
   * @param {boolean} startExclusive is range exclusive from left side
   * @param {boolean} endExclusive is range exclusive from right side
   * @param {boolean} logic is not logic applied
   * @returns {string}
   */
  static formatRange(
    start: classNum,
    end: classNum,
    startExclusive: boolean,
    endExclusive: boolean,
    logic: boolean
  ): string;
  /**
   * @param {Array<RangeValue>} values
   * @param {boolean} logic is not logic applied
   * @return {RangeValue} computed value and it's exclusive flag
   */
  static getRangeValue(
    values: Array<[classNum, boolean]>,
    logic: boolean
  ): [classNum, boolean];
  /** @type {Array<RangeValue>} */
  _left: Array<[classNum, boolean]>;
  /** @type {Array<RangeValue>} */
  _right: Array<[classNum, boolean]>;
  /**
   * @param {classNum} value
   * @param {boolean=} exclusive
   */
  left(value: classNum, exclusive?: boolean | undefined): void;
  /**
   * @param {classNum} value
   * @param {boolean=} exclusive
   */
  right(value: classNum, exclusive?: boolean | undefined): void;
  /**
   * @param {boolean} logic is not logic applied
   * @return {string} "smart" range string representation
   */
  format(logic?: boolean): string;
}
declare namespace Range {
  export { RangeValue, RangeValueCallback };
}
type RangeValue = [classNum, boolean];
type RangeValueCallback = (rangeValue: [classNum, boolean]) => boolean;

declare type Task<T> = (done: () => void) => Promise<T>;
interface Pool {
    submit<T>(task: Task<T>): Promise<T>;
    size: classNum;
    readonly pending: classNum;
}
declare function createPool(size: classNum): Pool;
export { Pool, createPool };
